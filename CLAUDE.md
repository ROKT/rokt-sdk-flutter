<!-- Claude Code reads CLAUDE.md, not AGENTS.md. This is a one-line import rather than a symlink,
     because git on Windows defaults to core.symlinks=false and would check a symlink out as a
     text file, which Claude Code would then read as literal text instead of following it.
     Keep all guidance in AGENTS.md. Never duplicate it here; the two copies would drift. -->

@AGENTS.md
